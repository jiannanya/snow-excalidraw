[project]
name = "snow-excalidraw-scripts"
version = "1.0.0"
requires-python = ">=3.11"

[project.dependencies]
requests   = ">=2.31.0"
playwright = ">=1.40.0"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.uv]
